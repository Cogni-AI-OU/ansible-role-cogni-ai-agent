---
# A file containing a list of collections to be installed.
# Usage:
# ansible-galaxy install -r requirements.yml
# Note: Keep dependencies sorted alphabetically.
collections:
  - name: ansible.posix
  - name: community.docker
  - name: community.general  # Required for apk, from_ini and supervisorctl modules.
  - name: https://github.com/Cogni-AI-OU/ansible-collection-github_models.git
    type: git
    version: main
